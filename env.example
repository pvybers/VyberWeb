DATABASE_URL="postgres://user:pass@host/db?sslmode=require"

# Optional (used for suggested actions; if missing, the app uses a simple fallback)
GEMINI_API_KEY=""
GEMINI_MODEL="gemini-2.5-flash"
GEMINI_IMAGE_MODEL="gemini-3-flash-preview"

# Video generation engine (use "kling" or "seedance")
VIDEO_GEN_MODEL="seedance"

# ByteDance Seedance (ARK)
SEEDANCE_API_KEY=""

# Kling AI (image-to-video, supports image_tail)
KLING_ACCESS_KEY=""
KLING_SECRET_KEY=""
KLING_TOKEN_TTL_SECONDS="1800"

